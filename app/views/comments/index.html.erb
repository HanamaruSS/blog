<p id="notice"><%= notice %></p>

<h1>Comments</h1>

<table>
  <thead>
    <tr>
      <th>Body</th>
      <th>Created</th>
      <th>Postid</th>
      <th>Userid</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.body %></td>
        <td><%= comment.created %></td>
        <td><%= comment.postid %></td>
        <td><%= comment.userid %></td>
        <td><%= link_to 'Show', post_comment_path %></td>
        <td><%= link_to 'Edit', edit_post_comment_path(comment) %></td>
        <td><%= link_to 'Destroy', post_comment_path, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

